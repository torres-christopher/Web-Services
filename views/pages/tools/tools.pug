extends ../../layouts/main

block content

  include ../../partials/tool-header

  .tool-layout
    .container

      .section-header
        h2.section-title Textové nástroje
        p.section-link Nástroje pro práci s textem — počítání znaků, převod velikosti písmen, čištění textu a další.

      .tools-grid

        each tool in tools
          if tool.enabled
            a.tool-card(href=tool.path)
              .tool-card__icon= tool.icon
              .tool-card__body
                h3.tool-card__name= tool.title
                p.tool-card__desc= tool.description
              span.tool-card__tag= tool.categoryName
          else
            .tool-card.tool-card--disabled
              .tool-card__icon= tool.icon
              .tool-card__body
                h3.tool-card__name= tool.title
                p.tool-card__desc= tool.description
              span.tool-card__tag Připravujeme
